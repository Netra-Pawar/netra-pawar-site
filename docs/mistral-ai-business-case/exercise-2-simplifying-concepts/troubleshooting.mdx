---
title: "Troubleshooting: Why Didn't My Assistant Find the Right Answer?"
description: Common embedding and retrieval issues for developers new to semantic search.
sidebar_label: Troubleshooting
sidebar_position: 3
---

# Troubleshooting: Why Didn't My Assistant Find the Right Answer?

When your retrieval system misses relevant content, the cause is rarely the embedding model itself. Work through the checklist below to isolate the problem.

## Diagnostic checklist

| Check | Question to ask |
|---|---|
| **Was the content indexed?** | Is the document in your vector store? Was it added after the last indexing run? |
| **Was it chunked well?** | Is the answer split across two chunks? Is the relevant paragraph buried inside a chunk with unrelated content? |
| **Was structure preserved?** | Did tables, FAQ widgets, or collapsible sections lose their structure during text extraction? |
| **Is the index fresh?** | Was the document updated since it was last embedded? Does the old version still appear? |
| **Is the query too vague?** | Does `"help with my account"` have enough meaning for the model to identify the right topic? |
| **Did filters exclude it?** | If you apply metadata filters (product, version, language), could they be removing the correct result? |
| **Is a near-duplicate outranking it?** | Do you have multiple similar documents (e.g., v1 and v2 of the same guide)? The wrong version might score higher. |

:::tip
Test retrieval separately from generation. Embed your query, inspect the top 5 returned chunks, and confirm the right content appears before looking at the generated answer. If the right chunk isn't retrieved, no amount of prompt engineering will fix the response.

Most vector stores and frameworks expose a search or query method that returns results with similarity scores — check your vector store's documentation for how to inspect raw results before they reach the model. If you're using [Libraries](/studio-api/knowledge-rag/libraries), you can test retrieval through the API without a full agent setup.
:::

## Common mistakes

**1. Confusing retrieval with generation.**
Embeddings retrieve content. The language model generates the answer. If your RAG application gives a wrong answer, the problem could be in retrieval (wrong chunk surfaced) or in generation (model misinterpreted the chunk). A related mistake: embedding entire documents instead of chunks, which averages out the meaning so specific questions match broad documents instead of the relevant paragraph. Split documents into focused chunks before embedding.

**2. Using embeddings for exact-match lookups.**
If the user asks for error code `ERR_AUTH_042`, embeddings will find semantically similar content about authentication errors — not the specific error code entry. Combine with keyword search or metadata filters.

**3. Forgetting that new content isn't searchable until it's embedded.**
If you publish new documentation but don't re-run your embedding pipeline, users won't find it. Embeddings are computed at indexing time, not at query time.

## Still stuck?

| Situation | Where to go |
|---|---|
| Need to debug a production pipeline stage-by-stage | [Production Retrieval Diagnostics →](../production-retrieval/diagnostics) |
| Need to measure retrieval quality with metrics | [Evaluation Metrics →](../production-retrieval/evaluation) |
| Want managed retrieval that handles chunking and indexing | [Libraries](/studio-api/knowledge-rag/libraries) |
| Want a chunking + hybrid search framework | [Search Toolkit](/studio-api/knowledge-rag/search-toolkit) |
